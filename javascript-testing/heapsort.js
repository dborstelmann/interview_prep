const heapsort = arr => {
  return arr
}

// const testArray = []
// for(let i = 0; i < 100000; i++) {
//   testArray.push(Math.random() * 1000)
// }

console.log(heapsort([5, 4, 7, 2, 1]));

// console.log(heapsort(testArray, 0, testArray.length - 1))
